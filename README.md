# Vasishta Urban Temples

Pure static HTML/CSS/JavaScript website.

Open `index.html` directly in a browser, or deploy the folder to Vercel/Netlify/GitHub Pages.

The contact form validates locally and opens the visitor email client with a prefilled enquiry to `info@urbantemples.in`; no backend/database is used.
